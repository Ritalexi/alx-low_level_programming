#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strcpy - Entry point
 * @dest: pointer
 * @src: pointer
 * Return: Always 0
 */

char *_strcpy(char *dest, char *src)
{
	_strcpy(dest, src);

	return (dest);
}
