#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * rev_string - Entry point
 * @s: pointer
 */

void rev_string(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
	}
	for (i = i - 1; i >= 0; i--)
	{
		s[i];
	}
}
