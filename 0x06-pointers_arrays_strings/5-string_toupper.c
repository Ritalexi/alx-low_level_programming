#include <stdio h>
#include "main.h"

/**
 * string_toupper - Entry point
 * Return: Always 0
 */

char *string_toupper(char *)
{
	a;
	int i;

	for (i = 0; [i] != '\0'; i++)
	{
		if ([i] >= 'a' && [i] <= 'z')
		{
			[i] = [i] - 32;
		}
	}
	return (0);
}
