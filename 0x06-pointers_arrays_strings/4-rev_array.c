#include <stdio.h>
#include "main.h"

/**
 * reverse_array - Entry point
 * @a: pointer
 * @n: input
 */

void reverse_array(int *a, int n)
{
	int i, j, k;

	i = 0;
	while (i < n)
	{
	i++;
	}
	for (j = l; j >= 0; j--)
	{
	k = a[j];
	}
}
