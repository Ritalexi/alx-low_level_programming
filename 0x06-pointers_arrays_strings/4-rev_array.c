#include <stdio.h>
#include "main.h"

/**
 * reverse_array - Entry point
 * @a: pointer
 * @n: input
 */

void reverse_array(int *a, int n)
{
	int i, j;

	i = 0;
	while (i < n)
	{
	i++
	}
for (j = n; j >= 0; j--)
{
	if (j == 0)
		continue;
	_putchar(a[j]);
	_putchar(',');
	_putchar(' ');
}
}
