#include "main.h"
#include <string.h>

/**
 * _strstr - Entry point
 * @haystack: pointer
 * @needle: pointer
 * Return: Always 0
 */

char *_strstr(char *haystack, char *needle)
{
	strstr(haystack, needle);

	return (0);
}
