#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int no;
	int i;
	char ch;
	char j;

	for (no = 0; no <= 9; no++)
	{
		i = no;
	}
	for (ch = 'a'; ch <= 'f', ch++)
	{
		j = ch;
	}
	putchar(ij);
	putchar('\n');
	return (0);
}
