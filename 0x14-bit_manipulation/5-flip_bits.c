#include "main.h"

/**
 * flip_bits - returns the number of bits you would
 * need to flip to get from one number to another
 * @n: number one.@n: number one.
 * @m: number two.
 * Return: number of bits.
 */
