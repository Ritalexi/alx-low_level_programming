#include "main.h"

/**
 * _isdigit(inct c) - Entry point
 * @c: input
 * Return: 0
 */

int _isdigit(int c)
{
	int i, j;

	for (i = 0; i <= 9; i++)
	{
		if (i == c)
		{
			j = 1;
			break;
		}
	}

	return (0);
}
